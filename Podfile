source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '18.3'

target 'HereAlready' do
  use_frameworks!
  # No pods — using system frameworks only (MapKit, CoreLocation, UserNotifications)
end
